name: server
description: A server app using the shelf package.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  dotenv: ^4.2.0
  shared_logic:
    path: ../../packages/shared_logic # This link is vital!
  shelf_static: ^1.1.3

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
