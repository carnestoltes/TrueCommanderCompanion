name: BEDH
version: 1.0.0
publish_to: 'none'
resolution: workspace 

environment:
  sdk: '>=3.5.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  url_launcher: ^6.3.2
  file_picker: ^10.3.10
  qr_flutter: ^4.1.0
  mobile_scanner: ^6.0.0
  http: ^1.1.0
  shared_logic:
    path: ../../packages/shared_logic

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/About.png 
